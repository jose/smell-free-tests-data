# Sanity check
report_file_path="$report_dir_path/statistics.csv"
[ -s "$report_file_path" ] || die "[ERROR] $report_file_path does not exist or it is empty!"
num_rows_stats_file=$(wc -l "$report_file_path" | cut -f1 -d' ')
[ "$num_rows_stats_file" -eq "2" ] || die "[ERROR] The statistics file ($report_file_path) generated by EvoSuite does not have the number of expected rows, i.e., two!"

